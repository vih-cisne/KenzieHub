import styled from "styled-components";

export const Work = styled.li`
    a {
        color: red;
        cursor: pointer;
        pointer-events: auto;
        white-space: nowrap;
    }

    p {
        cursor: pointer;
        pointer-events: auto;
    }

    div {
        align-items: center;

    }

`